# Python distribution (planned)

Platform wheels will carry the same Rust binary and expose diskwhy. A small Python helper may invoke JSON commands. No second scanning engine and no mandatory Rust compiler for supported wheels.

No pyproject.toml or launcher is shipped until binary packaging is implemented and registry names are selected. M4 owns clean-install tests, supported platforms, license metadata and publication.
