# npm distribution (planned)

A thin launcher with platform packages will expose the Rust diskwhy binary to humans and agents through npm/npx. JSON results come from the core engine.

No placeholder package.json or failing installer is published. M4 selects available names, builds binaries, tests clean installs and configures registry publication.
